<assembly xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/assembly-1.0.0.xsd">

	<includeBaseDirectory>false</includeBaseDirectory>
	<formats>
		<format>tar.gz</format>
	</formats>

	<fileSets>
			<fileSet>
			<directory>target</directory>
			<outputDirectory>${jar_installation_prefix}/${artifactId}</outputDirectory>
			<includes>
				<include>*.jar</include>
			</includes>
		</fileSet>
		<fileSet>
			<directory>etc</directory>
			<outputDirectory>${conf_installation_prefix}/${module_name}</outputDirectory>
		</fileSet>
		<fileSet>
			<directory>doc</directory>
			<outputDirectory>${doc_installation_prefix}/${artifactId}-${version}</outputDirectory>
		</fileSet>
		<fileSet>
			<directory>.</directory>
			<outputDirectory>${doc_installation_prefix}/${artifactId}-${version}</outputDirectory>
			<includes>
				<include>LICENSE.txt</include>
			</includes>
		</fileSet>
	</fileSets>

	<dependencySets>
		<dependencySet>
			<outputDirectory>${jar_installation_prefix}/${artifactId}</outputDirectory>
			<excludes>
				<exclude>it.grid.storm:storm-gridhttps-plugin</exclude>
			</excludes>
		</dependencySet>
	</dependencySets>
</assembly>
